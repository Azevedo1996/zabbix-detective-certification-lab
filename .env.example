# Exemplo de variáveis de ambiente, caso o projeto seja evoluído para produção.
# Este projeto educacional usa SQLite local por padrão.

DJANGO_DEBUG=True
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
